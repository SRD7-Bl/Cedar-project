# Cedar Point System 使用手册

## 1. 快速开始

本系统包含两个独立入口：

- `PMS.exe`：积分管理系统
- `GMS.exe`：物资管理系统

推荐交付结构：

- `PMS/`
- `PMS/PMS.exe`
- `PMS/.env`
- `GMS/`
- `GMS/GMS.exe`
- `GMS/.env`

只要打包文件完整，且 `.env` 与 `.exe` 放在同一目录，新电脑通常可以直接使用。

使用前请确认：

- 电脑可以联网
- 可以访问飞书 Open API
- 本机 `8787` 端口没有被其他程序占用
- 电脑安装了默认浏览器

启动方式：

1. 双击 `PMS.exe` 或 `GMS.exe`
2. 等待几秒钟，让本地后端启动
3. 系统会自动在浏览器中打开对应页面
4. 屏幕上会保留一个小的启动器窗口

注意：

- 使用过程中不要关闭启动器窗口
- 关闭启动器窗口会同时关闭本地后端

## 2. PMS：积分管理系统

### 2.1 PMS 的用途

PMS 用于管理学生积分。

典型用途：

- 查看学生列表
- 查看积分排名
- 新增积分事件
- 修改已有积分事件
- 查看积分历史记录

### 2.2 常规使用流程

推荐日常操作顺序：

1. 打开 `PMS.exe`
2. 等待页面加载完成
3. 选择目标学生
4. 输入积分变化
5. 填写类型、说明和操作人
6. 提交并检查结果

### 2.3 常见操作

新增积分：

1. 找到目标学生
2. 输入正数积分
3. 选择或填写原因
4. 点击提交

扣除积分：

1. 找到目标学生
2. 输入负数积分，或使用页面中的扣分操作
3. 填写原因
4. 点击提交

修正积分记录：

1. 找到需要修改的积分事件
2. 修改错误字段
3. 点击保存
4. 检查学生总积分是否正确更新

刷新数据：

- 如果页面内容看起来不是最新的，可以手动刷新浏览器页面
- 如果刚启动程序，先等待几秒让数据同步完成

### 2.4 PMS 使用注意事项

- 学生最终积分是由积分事件累加得到的
- 如果积分不对，优先检查事件记录是否录入错误
- 网络较慢时，不要连续重复点击提交
- 修改事件后，最好同时检查事件记录和学生总积分

## 3. GMS：物资管理系统

### 3.1 GMS 的用途

GMS 用于管理商品库存和学生积分兑换。

它包含两个主要界面：

- `Inventory`：库存管理
- `Shop`：兑换与结账

### 3.2 库存管理界面

库存界面用于管理：

- 商品名称
- 商品价格
- 商品数量
- 图片地址
- 备注信息

常见操作：

新增商品：

1. 打开 `GMS.exe`
2. 保持在 `Inventory` 标签页
3. 点击添加按钮
4. 填写名称、价格、数量、图片地址和备注
5. 点击保存

编辑商品：

1. 找到商品卡片
2. 点击 `编辑`
3. 修改对应字段
4. 点击保存

增加库存：

1. 找到商品卡片
2. 点击 `+`
3. 确认数量已更新

减少库存：

1. 找到商品卡片
2. 点击 `-`
3. 如果数量减到 `0`，商品可能会被自动删除

删除商品：

1. 打开商品编辑框
2. 点击删除
3. 确认该商品已经从库存中移除

### 3.3 商店兑换界面

商店界面用于学生积分兑换。

标准结账流程：

1. 切换到 `Shop`
2. 选择学生
3. 查看当前积分
4. 把商品加入购物车
5. 确认总件数和总价格
6. 填写操作人和备注
7. 点击结账

结账成功后：

- 学生积分会被扣除
- 商品库存会减少
- 购物车会自动清空

### 3.4 Shop 使用注意事项

- 系统在结账前会重新同步库存和积分
- 如果库存发生变化，购物车数量可能会被自动修正
- 如果学生积分不足，结账会失败
- 如果购物车为空，系统不会执行结账

### 3.5 GMS 使用建议

- 商品名称尽量统一
- 商品价格和库存数量尽量准确
- 特殊商品建议填写备注
- 大量兑换前建议先点一次同步
- 每次结账后，最好核对库存和学生积分

## 4. 常见问题

### 4.1 程序启动后没有自动打开页面

可能原因：

- 没有设置默认浏览器
- 系统阻止了浏览器自动打开

处理方法：

- 保持启动器窗口不要关闭
- 手动打开浏览器并访问：
  - PMS：`http://127.0.0.1:8787/app/pms`
  - GMS：`http://127.0.0.1:8787/app/gms`

### 4.2 后端启动失败

可能原因：

- `.env` 缺失
- `8787` 端口被占用
- 电脑无法访问飞书
- 打包文件不完整

处理方法：

1. 检查 `.env` 是否和 `.exe` 在同一目录
2. 关闭其他已经打开的 PMS 或 GMS
3. 重新启动程序
4. 如果目录中存在 `launcher_backend.log`，优先检查这个日志文件

### 4.3 数据加载失败

可能原因：

- 网络异常
- 飞书配置错误
- `.env` 不是正确版本

处理方法：

1. 检查电脑网络
2. 确认交付的 `.env` 正确无误
3. 关闭程序后重新打开

### 4.4 页面能打开，但按钮点击后没有反应

处理方法：

1. 等待几秒，确认请求是否仍在处理中
2. 手动刷新浏览器页面一次
3. 如果仍然异常，关闭程序重新启动

## 5. 建议培训顺序

对于第一次使用的人，建议按这个顺序上手：

1. 学会打开和关闭 `PMS.exe`
2. 学会录入一条积分事件
3. 学会修改一条积分事件
4. 学会打开和关闭 `GMS.exe`
5. 学会新增一个商品
6. 学会完成一次结账兑换

## 6. 管理员说明

对于负责发程序的人：

- 请交付整个打包目录，不要只发一个 `.exe`
- 请确保 `.env` 和 `.exe` 放在同一目录
- 如果 PMS 和 GMS 使用不同配置，请分别放置各自的 `.env`
- 更新程序时，建议整体替换整个文件夹
